#ifndef CALCULATOR
#define CALCULATOR

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>

void menu();
double addition(int argument, ...);
double subtraction(int argument, ...);
double multiplication(int argument, ...);
double division(int argument, ...);
int main();

#endif
